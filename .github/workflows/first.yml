name: Hello World Workflow

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print Hello, World!
        run: echo "Hello, World!"

      - name: Print Hello, World123!
        run: echo "Hello, World123123123123123!"
